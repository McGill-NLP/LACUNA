Full:
  data_path: ${paths.memorized_datasets_dir}
  name: 'Phone_Number'
  split: 'full'
  handler: QADataset
  args:
    hf_args:
      path: ${data.full.Full.data_path}
      name: ${data.full.Full.name}
      split: ${data.full.Full.split}
