[project]
name = "llm-unlearn"
version = "0.1.0"
description = "Unlearning tools for LLMs"
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["."]